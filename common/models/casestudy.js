// var GeoJSON = require('geojson');

module.exports = function(Casestudy) {

    // Casestudy.afterRemote('**', function(ctx, casestudy, next) {

    //     var mapped = []
    //       , parsed;

    //     mapped = ctx.result.map( function( result, idx ){
    //         return result.__data;
    //     });

    //     parsed = GeoJSON.parse( mapped, {
    //         Point: 'coordinates'
    //     });

    //     ctx.result = parsed;

    //     next();
    // });
};
